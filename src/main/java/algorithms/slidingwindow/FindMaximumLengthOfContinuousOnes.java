package algorithms.slidingwindow;

public class FindMaximumLengthOfContinuousOnes {
    public static int findMaximumLength(int[] A) {

        //declare begin and end

        int begin = 0;
        int end = 0;

        //declare sliding window
        int[] window = new int[2];

        for(int low = 0, high = 0;high < A.length - 1;high++){

            if(window[high] > 1){

            }
        }



        return 0;
    }
}
