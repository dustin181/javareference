package algorithms.slidingwindow;

public class FindLongestWithAtLeastKRepeating {
    public static int findLongest(String s, int k) {

        //get length of string
        //if length less than k, return 0
        //set left and right pointers
        //create HashMap to hold characters

        //while loop for length
        //if map doesnt contain char
        //increment value in map
        //else

        return 0;
    }
}
